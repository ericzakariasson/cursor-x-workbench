# cursor-x-workbench

A tiny Next.js app that renders a cute terminal demo with animated command output.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

The homepage automatically plays the demo. Use the "Restart demo" button to replay it at any time.

## Build

```bash
npm run build
npm run start
```
