# Snake Game

Small browser-based Snake game inspired by the Nokia classic.

## Run locally

1. From the repo root, start a simple static server:
   `python3 -m http.server 8000`
2. Open `http://localhost:8000` in your browser.
3. Use arrow keys or WASD to move. Click Restart to reset.
